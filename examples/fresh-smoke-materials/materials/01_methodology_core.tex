\section{Methodology Core}
\SystemName{} is a staged manuscript-support workflow. It snapshots author inputs, creates an outline, asks separate lanes to prepare plot and literature artifacts, and then writes a draft using only the registered artifacts. The method claim for this smoke test is limited to this artifact pipeline, its validation checkpoints, and its handoff boundaries.
