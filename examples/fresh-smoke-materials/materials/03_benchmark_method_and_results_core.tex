\section{Evaluation Core}
The smoke evaluation uses a deterministic local workload with three pipeline stages: planning, writing, and validation. The local run records 3 operator review attempts, 0 promoted candidate rewrites, and a final loop verdict requiring human review. These numbers are test fixtures for orchestration validation only; they are not claims about external model quality or runtime performance.
